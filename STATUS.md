# Release status

Current status: **Candidate — E2E fine-tuning source and local CUDA pre-flight complete, awaiting exact-blob clean-runtime execution**. The pipeline performs bounded real-gradient adaptation of the metric head, evaluates a deterministic held-out split against a constant-median baseline, exports a manifest-bound SafeTensors adapter, and reconstructs a fresh pinned base model for reload verification. The standalone `E2E` notebook is generated from `tools/notebook_template.py` under DIMER Notebook Specification 2.0 and completed 11/11 code cells locally on CUDA. Unit/static checks and that version-deviating workstation run are not supported-runtime notebook evidence; promotion remains blocked until the exact committed notebook blob passes a clean Colab or Kaggle `Run all` recorded in `docs/release-verification.md`.
