# Release status

Current status: **Candidate — fresh exact-blob Kaggle T4 E2E execution required after review remediation**. The pipeline performs bounded real-gradient adaptation of the metric head, evaluates a deterministic held-out split against a constant-median baseline, exports a manifest-bound SafeTensors adapter, and reconstructs a fresh pinned base model for reload verification. The v4 Kaggle run remains an immutable historical record, but it predates the latest leakage, input-normalization, bounded-memory, pixel-weighting, optimizer-provenance, and artifact-integrity corrections and cannot qualify the current carrier. Requalification requirements are recorded in `docs/release-verification.md`; no promotion has occurred.
