# Release status

Current status: **Candidate — tutorial source complete, local pre-flight executed, awaiting supported-runtime execution**. The pipeline package, offline unit tests (including the role-helper, import-boundary and notebook-parity suites), a local CPU smoke run, `MODEL_CARD.md` (MODEL_CARD_SPEC 1.1), and the standalone `TASK-INFERENCE` tutorial `tutorials/zoedepth_metric_depth_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 2.0 §4) exist and pass static validation (`tools/validate_release_assets.py`, including the generator parity checks PAR1–PAR3); one fresh-kernel execution on the workstation is recorded in `docs/release-verification.md`. None of that is supported-runtime notebook evidence; the repository is not release-grade until a Colab/Kaggle execution for the exact candidate commit is recorded there.
